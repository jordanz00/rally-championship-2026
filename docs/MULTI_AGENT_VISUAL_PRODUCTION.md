# Rally Championship 2026 — Multi-Agent AAA Visual Production (Cursor master)

**Canonical detail:** [`docs/AGENT_ARCHITECTURE.md`](AGENT_ARCHITECTURE.md) · [`docs/ART_DIRECTION.md`](docs/ART_DIRECTION.md) · [`docs/AAA_VISUAL_TARGET.md`](docs/AAA_VISUAL_TARGET.md)

## One-paragraph rule

Use specialist agents with **clear ownership**. Optimize **perceived visual quality / GPU ms**. Parallelize only when file/system ownership does not conflict. **Art Director (Agent 12)** may reject. **Integration (Agent 13)** merges only after `qa-validate` + geometry + perf + visual scores on **all four stages**. Never “all agents make graphics AAA” in one prompt. Never casually rewrite another agent’s systems. Primitives = PLACEHOLDER only. Shared tech; different stage art direction.
